# Design Source & Brand Files

These are **working / source files** that are **not used by the live website**.
They are kept here for reference so the project root stays clean.

Nothing in this folder is deployed (see `vercel.json` — only `index.html`,
`styles.css`, `script.js`, `server.js`, `assets/`, `admin/`, and `tools/` ship).

| File | What it is |
|------|------------|
| `Enso no Sato pdf profile.pdf` | Brand / restaurant profile document |
| `Enso no Sato esthetics.png` | Brand aesthetic / mood reference |
| `logo-original.png` | Original logo export (the live site uses `assets/logo.png`) |
| `logo-duplicate.png` | Duplicate logo export, kept for reference |
| `edit later.png`, `edit later (2).png`, `edit_later__2_-removebg.png` | In-progress logo/graphic edits |

The live logo used by the site lives at `../assets/logo.png`.
